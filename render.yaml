services:
  - type: web
    name: prescriptionguard-ai
    env: python
    buildCommand: "sudo apt-get install -y tesseract-ocr && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
